// Entry point for Expo Router.
// The main entry is set to "expo-router/entry" in package.json,
// so this file is kept for compatibility but is not the active entry point.
import 'expo-router/entry';
